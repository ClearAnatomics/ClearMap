"""
This subpackage handles the text configuration files for ClearMap.

The configuration files are used to store the main parameters of ``ClearMap``
and are used by the classes and GUI to configure the behaviour of the pipelines.

This subpackage contains the default configuration files and the classes to
parse them.

"""